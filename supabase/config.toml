project_id = "vlojfdgcbokneeammddo"

[functions.elevenlabs-tts]
# Required for signing-keys projects; auth is validated in code when needed.
verify_jwt = false

[functions.submit-ebook-lead]
# Public endpoint - rate limiting and validation done in code
verify_jwt = false